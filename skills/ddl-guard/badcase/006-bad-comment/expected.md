# ddl-guard badcase 006 — 注释不规范

check: ddl_check.py + 人工判断（ddl-manual-rules.md）

## 违规语句

### 表 1: t_order_no_comment
| 问题 | 说明 |
|---|---|
| 表注释缺失 | 表缺少 COMMENT 子句 |
| 字段注释缺失 | `order_no`, `buyer_id` 字段缺少 COMMENT |

### 表 2: t_order_very_long_comment
| 问题 | 说明 |
|---|---|
| 表注释长度超过 64 | 表注释长度超过 64 个字符 |

### 表 3: t_order_missing_field_comment
| 问题 | 说明 |
|---|---|
| 字段注释缺失 | `order_no`, `buyer_id` 字段缺少 COMMENT |

### 表 4: t_order_long_field_comment
| 问题 | 说明 |
|---|---|
| 字段注释长度超过 128 | 字段 `order_no` 注释长度超过 128 个字符 |

### 表 5: t_order_fullwidth_comment
| 问题 | 说明 |
|---|---|
| 字段注释含全角字符 | `order_no`, `buyer_id` 字段注释包含全角括号 `（）` |

### 表 6: t_order_hash_comment
| 问题 | 说明 |
|---|---|
| 使用 # 注释 | 文件级使用了 `#` 注释符号 |
| 注释格式 | 文件级使用了 `--` 注释但后无空格 |

### 表 7: t_order_no_space_comment
| 问题 | 说明 |
|---|---|
| 注释格式 | `--` 注释后缺少空格（应为 `-- `） |

### 表 8: t_order_bad_del_flag
| 问题 | 说明 |
|---|---|
| 逻辑删除字段名不规范 | 使用 `is_deleted` 而非规范的 `del_flag` |
| 逻辑删除字段注释不规范 | 注释格式不符合 `删除标志[0-否,1-是]` |

## 人工判断规则（脚本无法覆盖）

根据 `ddl-manual-rules.md`，以下规则需人工逐表核对：

1. **注释质量**：圆括号内容须为必要补充；冗余或让含义混淆的不写
2. **字段与注释对应**：字段名与注释含义对应、词汇对应
3. **设计合理性**：复数形式、字符串长度、数据行长度、NOT NULL + 默认值

## 预期检查输出

- 脚本自动检出：表注释缺失、字段注释缺失、表注释长度、字段注释长度、全角字符、注释符号、注释格式、逻辑删除字段名、逻辑删除字段注释
- 人工补充：注释语义、字段与注释对应关系
- 人工补充规则：补充信息必要性、字段与注释对应