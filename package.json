{
  "name": "awesome-rules",
  "version": "0.5.0",
  "private": true,
  "scripts": {
    "test": "bash scripts/run_tests.sh",
    "setup:hooks": "lefthook install",
    "release": "npx --no-install commit-and-tag-version",
    "release:dry": "npx --no-install commit-and-tag-version --dry-run"
  },
  "devDependencies": {
    "commit-and-tag-version": "13.1.2"
  }
}
