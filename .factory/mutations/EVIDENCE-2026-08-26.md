# Mutation kill-rate 证据 — 2026-08-26

- 命令：`python3 .factory/mutations/run.py`（全量，无 --only 过滤）
- 退出码：**0**（全部按预期；无 FAIL / SKIP / 配置错误；还原零残留——runner 内置逐文件字节校验通过）
- 缺陷集：15 条 = 篡改类 7（guard 门，含 G-06 点文件周界正向）+ 行为破坏类 5（tests 门）+ 负例 3（guard 2 + tests 1）
- 前一版证据：[EVIDENCE-2026-08-24.md](EVIDENCE-2026-08-24.md)（13 条 / 11-11 口径）；本次增量 = G-05（normalize 点前缀退化实证，expect_block=false 语义）+ G-06（`.gitignore` 点文件周界正向拦截）——ADR-007 移植实测的绕过面自此有 kill 证据

## 汇总（原文摘录）

```
===== mutation 冒烟汇总 =====
  [G-01] PASS       blocked=True（rc=1）符合预期
  [G-02] PASS       blocked=True（rc=1）符合预期
  [G-03] PASS       blocked=True（rc=2）符合预期
  [G-04] PASS       blocked=True（rc=1）符合预期
  [M-01] PASS       blocked=True（rc=2）符合预期
  [M-02] PASS       blocked=True（rc=2）符合预期
  [B-01] PASS       blocked=False（rc=0）符合预期
  [B-101] PASS      blocked=True（rc=1）符合预期
  [B-102] PASS      blocked=True（rc=1）符合预期
  [B-103] PASS      blocked=True（rc=1）符合预期
  [B-104] PASS      blocked=True（rc=1）符合预期
  [B-105] PASS      blocked=True（rc=1）符合预期
  [B-106] PASS      blocked=False（rc=0）符合预期
  [G-05] PASS       blocked=False（rc=0）符合预期
  [G-06] PASS       blocked=True（rc=1）符合预期
  篡改类拦截（guard 门）: 7/7 = 100%
  行为破坏类拦截（tests 门）: 5/5 = 100%
  正向缺陷拦截（kill rate）: 12/12 = 100%
  负例放行: 3/3
  结论: 门灵敏度冒烟通过（auto-merge 的必要非充分条件）
  周界指纹已绑定: 98d1a765e7f4（evidence-stamp.json）
```

## G-05 语义说明（本版新增，防误读）

G-05 注入的是**门自身缺陷**（normalize `lstrip("./")` 点前缀退化——ADR-007 移植实测：`.factory/` 周界被剥成 `factory/` 整体失明）。该缺陷形态下失明门**必然放行**，故 `expect_block=false` 钉死此行为：若未来 guard 对该注入反而拦截（rc≠0），说明 normalize 或前缀匹配被改宽，须人审。修复态的正向 kill 证据由 **G-06** 承载（`.gitignore` 点前缀触碰，正确 guard 必拦 rc=1）。
