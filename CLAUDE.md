# Awesome Rules — 项目规范与技能

本仓库提供一组可复用的研发规范（steering）和 AI Agent 技能（skills）。
在编写或审查代码时，**主动读取并遵守**以下规范文件。

规范分两组，体系独立，按当前任务所属体系选择对应规范，勿混用：

- **通用设计规范**（设计阶段）：`steering/*.md` —— API 设计、数据库设计、测试、Git 提交、审查报告输出、跨仓契约兼容性
- **GTSP 工程规范**（编码阶段，Java/Spring Cloud）：`steering/gtsp/*.md`，总入口 `steering/gtsp/README.md` —— 覆盖 `gtsp-*`/`fss-*` 微服务的项目结构、分层架构、命名、Feign、MyBatis、日志、异常、配置等

> 完整规范索引由 SessionStart hook（`hooks/load-steering.sh`）动态扫描各文件 frontmatter 生成并注入上下文。新增规范只需带 frontmatter（`title` + `scenario`），无需手动维护本文件。

## 审查技能（触发式）

- `/ddl-guard` — DDL/SQL 自动审查（脚本 + 人工判断）
- `/api-guard` — 业务接口自动审查（脚本 + 人工判断）
- `/arch-guard` — DDD 架构分层守护（脚本 + 人工判断）
- `/impact-guard` — 变更影响分析（改码前预估 + PR/CI 门禁，🔴直接/🟠间接分级）
- `doc-gen` — DDD 单项目文档站生成；多项目聚合归 `arch-hawkeye/`（全局观测与治理独立工程，契约见 `arch-hawkeye/AH-MANIFEST.md`）

## 使用原则

- 供人工勾选/终裁的物料清单必须程序化全量枚举（脚本提取+连续编号），不得凭模型阅读记忆罗列：记忆罗列存在随机遗漏（2026-09 实证：37 条 lessons 手工列清单漏 1 条，靠"其余默认"规则兜住），遗漏项会直接绕过人工审核进入下一环节。
- 对不熟悉的仓库脚本，`--help` 不是安全探针：脚本可能没有 argparse 分发、顶层直接执行（2026-09 实证：`release_guard.py --help` 直接跑完 catv 提交了版本 bump）。调用前先静态确认入口结构：`head -40 <脚本>` 看是否有 `if __name__` 分发/子命令，或 `grep -n 'argparse\|sys.argv' <脚本>`；只读意图的探查用源码阅读替代执行。

- 测试突现不明失败时，先排查并行进程对仓库的篡改，再归因自己的改动：核对 `git config core.bare` 是否被置 true、有无未知 worktree（`git worktree list`）。2026-08-27 实证：主仓被置 bare 致 hermetic 套件 4 例连带失败，恢复 false 后 4/4 复绿，与当次 diff 无关。归因更正（08-28 日志核验）：dispatch.log 证明 .factory 主链清白——作案发生在 13:13–13:23 两轮调度空档，系流水线调度的 agent 会话误操作；最初条款误记「流水线所为」是把在场当因果，教训见 skill-evo 审核第 4 点（归因断言须时间窗证据定案）。
- 集成类需求（消息桥接、外部系统对接等）先找现成插件再谈自研：动手写代码前必须先搜索目标生态的插件市场/包管理器（含上游项目的适配分发版），存在可用插件则优先安装验证，确认无合适插件后才允许自研，避免重复造轮子。

- 并行子任务按文件所有权隔离（各 prompt 的目标文件集无交集），子任务检出的边界外残留由主会话统一收口而非各自扩权；派发期间需拉取远程更新时，优先选子任务尚未写盘的窗口，若已写盘须先比对未提交修改与 incoming 改动的文件交集再决定拉取方式。
- herdr 并行派发子任务：每 pane 跑 headless agent（如 `omp -p --no-session --max-time 30m "$(cat /tmp/prompt-x.md)" > /tmp/x.out 2>&1; echo "X_EXIT=$?"`），主会话用 `pane wait-output --regex "X_EXIT=[0-9]+"` 等待完成；正则须含数字段，否则会误匹配命令回显里未展开的字面 `$?`；输出文件只有 `Working...` 是工作态而非失败，外层 run 命令超时也不代表 pane 内任务结束。

- 文档不写易腐数字：测试数量、组件数量等随代码变动的计数值不进 README/SKILL.md，改写为可执行命令（如 `pytest <测试目录> -q`）让事实自证；此类存量陈述由 doc-freshness 门禁守护，新增漂移会被 CI 拦截。
- 发现「实现与文档不一致」类问题时，优先接线而非只修文档：给守门层（`tools/check_doc_freshness.py` 及 gauntlet）追加规则，让门禁机械化拦截同类漂移再发；人工同步只做一次性存量清理，不新增靠人工纪律长期维持的一致性约定。

- 提交非本会话产出的变更（工作区遗留、并行会话落盘）：先核实 diff 与预期一致再提交，并在交付说明中披露归属与裁决来源——不把他人/并行会话的裁决冒充为本会话结论，含灰区裁决的推进须留人工再裁量窗口
- 标注强制的条款应同步评估可机械检查性：能查出的配 gauntlet 静态门 + 负控制（证明检查器会失败），门禁查不出违规的强制条款只靠人记，效力弱。

- 工厂链运行期间（`.factory/fix-issue.sh` 进程存活时）不得修改 `.factory/` 下任何文件：bash 按字节偏移增量解析脚本，运行中改写会使链在下一节点返回后读到错位内容而崩溃（2026-08 issue #2 实证：implement 节点返回后报 `line 113: syntax error`）。改动链脚本前先确认无存活链进程（如 `pgrep -f fix-issue.sh`）。
- 标注强制的条款应同步评估可机械检查性：能查出的配 gauntlet 静态门 + 负控制（证明检查器会失败），门禁查不出违规的强制条款只靠人记，效力弱。

- 工厂链运行期间（`.factory/fix-issue.sh` 进程存活时）不得修改 `.factory/` 下任何文件：bash 按字节偏移增量解析脚本，运行中改写会使链在下一节点返回后读到错位内容而崩溃（2026-08 issue #2 实证：implement 节点返回后报 `line 113: syntax error`）。改动链脚本前先确认无存活链进程（如 `pgrep -f fix-issue.sh`）。
- 设计文档统一存放于 `docs/design/` 并同步登记 README 索引表；写盘前先核对该目录已存在，禁止在仓库根新建平行目录（曾误建 `design/` 导致链接断链）

- 在相关任务出现时，先读取对应的 steering 规范，再开始工作
- 遵守各项规范；标注【强制】的条款不可违反（不通过则不予合并），【推荐】尽可能遵守
- 审查类技能会运行自动化脚本，不要跳过脚本检查步骤

## 交付前审查（强制）

产生代码/脚本/文档变更的任务，宣布完成前必须：

1. 派发一次独立审查子代理（omp 会话：task 工具 `agent: "reviewer"`）
   审查本任务全部 diff
2. 审查发现的可操作问题：修复后复审；确不处理的，列为交付说明中的
   显式遗留项
3. 收尾思考或总结中承认的未处理边界、假设、风险，同样按第 2 条处置——
   不允许只在思考里提及后交付

纯问答/咨询类任务豁免。
