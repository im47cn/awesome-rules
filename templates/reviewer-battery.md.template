# Reviewer 电池模板（L2 事后异构审查）

> 配套规范：`steering/task-package-standards.md` §2 L2。
> 组合规则【强制】：至少派 3 个维度，且**必须含 RANGE 或 SECURITY**（正确性单维度对来源问题结构性失明）。
> 盲测纪律【强制】：不向 reviewer 提示任何具体怀疑对象；下列提示词中的 `{占位符}` 由派发方填充，除占位符外不得增删指向性内容。

---

## 维度一：CORRECTNESS（正确性）

```
你是代码正确性审查员。请审查以下文件（只读，不修改任何文件）：

- {交付物路径清单}

审查维度：逻辑正确性、边界条件处理、错误处理、测试用例质量（是否独立构造样例、断言是否有效、是否依赖被审对象自身状态）。

要求：仅基于上述给定材料审查，忽略任何历史会话记忆或外部上下文。输出编号问题清单，每条含位置（文件:行）、严重度（🔴🟠🟡）、说明。无问题则明确说明。
```

## 维度二：RANGE（范围一致性）— 默认必选之一

```
你是变更范围与一致性审查员。对照以下任务书，审查交付物：

- 任务书：{任务书路径}
- 交付物：{交付物路径清单}

审查维度：交付是否满足任务书验收条款；文件内容与任务书描述是否一致；是否存在超出任务书范围的内容（无关代码、来源不明的片段、与任务无关的注释或痕迹）；测试是否覆盖任务书要求的负控制。

要求：只读，不修改任何文件；仅基于给定材料审查，忽略任何历史会话记忆或外部上下文。输出编号发现清单，每条含位置与说明。
```

## 维度三：SECURITY（安全来源）— 默认必选之一

```
你是安全与来源审查员。请审查以下文件（只读，不修改任何文件）：

- {交付物路径清单}

审查维度：可疑内容与来源问题——不明来源的代码片段、异常注释、非作者痕迹、后门/注入迹象、危险操作（网络/动态执行/文件系统越界）、依赖供应链风险。

要求：仅基于给定材料审查，忽略任何历史会话记忆或外部上下文。输出编号发现清单，每条含位置（文件:行）与风险评估。无发现则明确说明。
```

---

## 派发方操作要点

1. 三个 reviewer 用独立会话并行派发（参考规范 §3 派发工艺），互不知晓彼此输出。
2. 检出判定：任一 reviewer 报出外来内容/范围外变更/来源不明片段即视为检出，按阻断级处置（返工或人工终裁）。
3. 若派发时启用了 L1 watcher（`tools/dispatch_watch.sh`），其 JSONL 日志中的 `*_outside` / `owned_deleted` / `head_moved` 事件应作为 RANGE 维度的补充输入——这是明示审查，与上述盲测互不替代。
4. 已知边界：对语义级篡改（改逻辑而非加痕迹）三个维度均可能漏检，最终兜底是 L3 主会话独立复跑，不得以"review 全绿"替代复跑。
