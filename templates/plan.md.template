# Plan: <标题>（from spec.md <路径/commit>）

## 变更文件清单（所有权边界，供并行分解）
| 文件 | 动作 | 负责人（agent） | 依赖 |
|---|---|---|---|
| <repo>/<path> | 新增/修改/删除 | agent-<名> | — |
| <repo>/<path> | 新增 | agent-<名> | agent-<名> 的 API 契约 |

跨 agent 共享接口契约（字段名/状态码/错误结构）在此冻结，写入 batch context 的 Contract 区。

## 实施顺序
1. <步骤> —— 覆盖条款: spec:claim-status-001, 002
2. ...

（并行前提：步骤间无文件重叠；重叠的步骤串行或合并到同一 agent）

## 风险与回退
| 风险 | 影响 | 缓解 |
|---|---|---|
| <风险> | <影响> | <缓解> |

## Proof（完成定义，可验证的客观判据）
- [ ] 所有条款测试通过（见 spec.md 验收矩阵，spec_check.py 核对）
- [ ] 覆盖率 >=95% 且为终局测量（全部语义变更后重跑；中途达标不算数）
- [ ] 真实启动验证：<命令 + 期望输出>
- [ ] 实现偏离本 plan 的，同 commit 更新 plan.md

## 偏离记录
<实现中与 plan 不一致处，写这里，禁止悄悄改行为>
